import redis

r = redis.Redis(host="redis", port=6379)

def get_user_cached(user_id):
    cached = r.get(user_id)
    if cached:
        return cached

    user = db.query(User).get(user_id)
    r.set(user_id, str(user.email))
    return user
